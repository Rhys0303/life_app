關於本專案 (Project Overview)
本專案為 Django 框架的實踐練習，旨在建立一個高效的 API 後端服務。透過 Django REST Framework，實現了對待辦事項 (Todo Items) 的 CRUD (增刪查改) 操作，並成功將專案容器化部署至 Docker 環境，以解決跨平台環境配置的相容性問題。

核心功能 (Core Features)

    API 接口開發：透過 DRF 提供標準化的 JSON 輸出，實現待辦事項的管理功能。

    Docker 容器化：將 Django 應用程式與 MySQL 資料庫封裝於容器中，確保部署流程自動化且一致。

    後台管理：整合 Django 內建 Admin，提供直觀的資料庫視覺化管理介面。


可以看到使用者登入畫面

<img width="624" height="486" alt="image" src="https://github.com/user-attachments/assets/7e44c23e-cde9-4897-9af8-8d17b7183f73" />
